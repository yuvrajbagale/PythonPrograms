 # What is the difference between add() and update() Functions in Set?
 # 1. we can use add() to add individual items to the set, where as we can we upadted() function to add multiple items to set.
 # 2. add() function can take only one arguments where as update() function can take any numbers of arguments but all arguments shoud be iterable objects.

